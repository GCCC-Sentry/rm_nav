# odom_to_px4 参数配置
odom_to_px4_node:
  ros__parameters:
    # 订阅的 ROS 里程计话题
    odom_topic: "odometry"
    # 发布到 PX4 的话题
    px4_odom_topic: "/fmu/in/vehicle_visual_odometry"
    # 启用 PX4 时间同步
    use_timesync: true
    # 启用 ENU→NED / FLU→FRD 坐标系转换
    convert_enu_to_ned: true
